import { AllowHtmlPipe } from './allow-html.pipe';

describe('AllowHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new AllowHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
